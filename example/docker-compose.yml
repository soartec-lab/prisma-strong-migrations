services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: writemate
      POSTGRES_PASSWORD: writemate
      POSTGRES_DB: writemate
    ports:
      - "5432:5432"
    volumes:
      - db_data:/var/lib/postgresql/data

volumes:
  db_data:
